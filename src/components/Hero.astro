---
---
<section class="hero">
  <div class="overlay">
    <header class="content">
      <h1>💧 Salva Cada Gota</h1>
      <p>
        La escasez de agua es un reto global. Únete a nuestra misión para
        proteger, ahorrar y valorar cada gota de este recurso vital.
      </p>
      <a href="#acciones" class="btn">Descubre cómo ayudar</a>
    </header>
  </div>
</section>

<style>
  /* ===== Reset y box-sizing ===== */
  *, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    overflow-x: hidden; /* ✅ Evita scroll horizontal */
    font-family: 'Arial', sans-serif;
  }

  /* ===== Hero Section ===== */
  .hero {
    position: relative;
    width: 100%;
    height: 100vh;
    background: url("https://images.unsplash.com/photo-1521207418485-99c705420785?ixlib=rb-4.0.3&auto=format&fit=crop&w=1350&q=80")
      no-repeat center center/cover;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    overflow-x: hidden; /* ✅ Evita scroll horizontal */
  }

  /* Overlay azul semitransparente */
  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 60, 128, 0.65);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
  }

  /* Contenido centrado */
  .content {
    position: relative;
    max-width: 800px;
    text-align: center;
    z-index: 2;
  }

  .content h1 {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 1rem;
    color: #ffffff;
    text-shadow: 2px 2px 5px rgba(0, 0, 50, 0.4);
  }

  .content p {
    font-size: 1.25rem;
    line-height: 1.8;
    margin-bottom: 2rem;
    color: #e0f2ff;
  }

  .btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background: linear-gradient(135deg, #0077ff, #00bbff);
    color: #fff;
    font-weight: 600;
    border-radius: 30px;
    text-decoration: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .btn:hover {
    transform: scale(1.05);
    box-shadow: 0 8px 25px rgba(0, 123, 255, 0.4);
  }

  /* ===== Responsive ===== */
  @media (max-width: 768px) {
    .content h1 {
      font-size: 2.2rem;
    }

    .content p {
      font-size: 1rem;
    }
  }

  @media (max-width: 480px) {
    .content h1 {
      font-size: 1.8rem;
    }

    .content p {
      font-size: 0.9rem;
    }

    .btn {
      padding: 0.6rem 1.2rem;
      font-size: 0.9rem;
    }
  }
</style>
