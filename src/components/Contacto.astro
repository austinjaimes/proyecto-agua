---
---
<section class="contacto" id="contacto">
  <div class="container">
    <h2>📬 Participa y Contáctanos</h2>
    <p>
      Únete a la campaña #SalvaCadaGotaIguala. Déjanos tus datos y mensajes
      para recibir consejos y participar en nuestras actividades de concientización.
    </p>

    <form 
      name="contacto" 
      method="POST" 
      data-netlify="true" 
      class="formulario"
    >
      <!-- Campo oculto para Netlify -->
      <input type="hidden" name="form-name" value="contacto">

      <input type="text" name="nombre" placeholder="Nombre completo" required>
      <input type="email" name="correo" placeholder="Correo electrónico" required>
      <textarea name="mensaje" placeholder="Tu mensaje" rows="5" required></textarea>
      <button type="submit">Enviar</button>
    </form>
  </div>
</section>

<style>
  .contacto {
    padding: 5rem 2rem;
    background: linear-gradient(to bottom, #b2ebf2, #e0f7fa);
    color: #004d40;
  }

  .container {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
  }

  .contacto h2 {
    font-size: 2.5rem;
    font-weight: 800;
    margin-bottom: 1rem;
    color: #0077b6;
  }

  .contacto p {
    font-size: 1.15rem;
    margin-bottom: 2.5rem;
  }

  .formulario {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .formulario input,
  .formulario textarea {
    padding: 0.8rem 1rem;
    border-radius: 10px;
    border: 1px solid #0077b6;
    font-size: 1rem;
    outline: none;
    transition: border 0.3s;
  }

  .formulario input:focus,
  .formulario textarea:focus {
    border-color: #00bbff;
  }

  .formulario button {
    padding: 0.8rem 1.5rem;
    border: none;
    border-radius: 30px;
    background: linear-gradient(135deg, #0077ff, #00bbff);
    color: white;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .formulario button:hover {
    transform: scale(1.05);
    box-shadow: 0 8px 20px rgba(0, 123, 255, 0.4);
  }

  /* Responsive */
  @media (max-width: 480px) {
    .contacto h2 {
      font-size: 2rem;
    }

    .contacto p {
      font-size: 1rem;
    }
  }
</style>
